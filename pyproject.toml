[project]
name = "block-quantenschaltung"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Marius", email = "whalter123a@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.11.2",
    "numpy>=2.5.3",
    "pre-commit>=4.6.2",
    "pytest>=9.1.1",
    "qiskit>=2.5.2",
    "qiskit-aer>=0.17.2",
]

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "sphinx>=9.1.0",
    "sphinx-rtd-theme>=3.1.0",
]
